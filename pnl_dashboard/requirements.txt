# All of these are ALREADY in your environment — nothing new to install.
# Versions below are the ones you have; the code is written to work with them.
pandas==1.2.4
plotly==5.13.0
openpyxl==3.0.7        # reads the .xlsx sample fallback
pyarrow==11.0.0        # optional: parquet cache for the sample data
SQLAlchemy==1.4.15     # optional: only if you use SQLAlchemy in _load_from_sql
# psycopg2 / pyodbc are also available if you connect via those drivers.
