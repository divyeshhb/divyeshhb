pandas>=2.0
plotly>=5.20
openpyxl>=3.1        # only needed for the Excel sample fallback
pyarrow>=14          # optional: parquet cache for the sample data
# sqlalchemy>=2.0     # uncomment when you wire up the SQL data source
